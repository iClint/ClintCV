export enum ContentType {
  IMAGES = 'images',
  TEXT = 'text',
  LIST = 'list',
  HEADING = 'heading',
  COMPONENT = 'component',
  VIDEO = 'video',
}

export enum PreviewSize {
  SMALL = 'small',
  MEDIUM = 'medium',
  LARGE = 'large',
}
